include ../make.inc
include ../libraries.inc
FC = $(MPIF90) 
### default
FFLAGS = $(MPIF90_OPTS) -Ifinclude
### debug
LD = $(FC)
LDFLAGS =$(MPIF90_OPTS) $(MPI_LIBS) $(INTERNALLIBS)
TMPFILES = *.mod
SUFFIX=mpi
