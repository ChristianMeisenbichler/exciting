include ../../make.inc
include ../../libraries.inc

FC = $(MPIF90)
FFLAGS = $(MPIF90_OPTS) -Ifinclude
LD = $(FC)
LDFLAGS =$(MPIF90_OPS) $(MPI_LIBS) $(INTERNALLIBS)
TMPFILES = *.mod
PREFIX=../../..
SUFFIX=utmpi
